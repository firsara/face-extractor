#!/usr/bin/env node

const path = require('path');
const execute = require('../src/utils/execute');

const [, , inputFile] = process.argv;

execute(`npm run --prefix ${path.resolve(path.join(__dirname, '..'))} extract ${inputFile}`);
